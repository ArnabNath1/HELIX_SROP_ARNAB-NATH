services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./chroma_db:/app/chroma_db
      - .:/app
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./helix_srop.db
      - CHROMA_PERSIST_DIR=/app/chroma_db
